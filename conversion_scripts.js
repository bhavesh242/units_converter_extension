function get_conversions(text){

    //Regular expression to match the numerical part and optional space
    const numSpaceRegExp = new RegExp(/((^[\-−]?(?:\d+|\d{1,3}(?:,\d{3})+)(?:(\.|,)\d+)?))\s*/);
    let result="";
    


}